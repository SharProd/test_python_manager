version: "3"
services:
  db:
    image: postgres
    container_name: manager_db
    volumes:
      - ./data/db:/var/lib/postgresql/data
    env_file:
     - .env.dev
#    environment:
#      POSTGRES_PASSWORD: postgres
    ports:
      - '5432:5432'
    expose:
      - '5432'
  web:
    build: .
    container_name: manager_web
    command: >
      bash -c "python manager/manage.py makemigrations
      && python manager/manage.py migrate
      && python manager/manage.py runserver 0.0.0.0:8080"
    volumes:
      - .:/code
    env_file:
      - .env.dev
    ports:
      - "8080:8080"
    depends_on:
      - db